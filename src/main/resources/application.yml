spring:
  datasource:
    url: jdbc:postgresql://localhost:5455/raven
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.format_sql: true
    show-sql: true
